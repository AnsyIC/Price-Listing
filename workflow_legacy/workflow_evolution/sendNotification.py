def sendNotification(to: str, subject: str, message: str):
    """Sends an email notification."""
    # Placeholder for Gmail API
    print(f"Sending email to {to}")
    print(f"Subject: {subject}")
    print(f"Message: {message}")
    return {"success": True}
